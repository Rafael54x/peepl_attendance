<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_attendance.hr_attendance_management_action" model="ir.actions.act_window">
        <field name="context">{}</field>
    </record>

    <record id="view_attendance_tree_management_custom" model="ir.ui.view">
        <field name="name">hr.attendance.list.custom</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.view_attendance_tree_management"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="replace">
                <list string="Employee attendances" create="1" import="1">
                    <header>
                        <button class="btn-secondary" string="Approve" name="action_approve_overtime" type="object"/>
                        <button class="btn-secondary" string="Refuse" name="action_refuse_overtime" type="object"/>
                    </header>
                    <field name="employee_id" widget="many2one_avatar_employee"/>
                    <field name="check_in"/>
                    <field name="check_out"/>
                    <field name="attendance_type"/>
                    <field name="worked_hours" string="Worked Time" widget="float_time" optional="show"/>
                    <field name="overtime_hours" string="Worked Extra Hours" widget="float_time" optional="show"/>
                    <field name="validated_overtime_hours" readonly="overtime_status == 'refused'" string="Extra Hours" widget="float_time" optional="show"/>
                </list>
            </xpath>
        </field>
    </record>

    <record id="hr_attendance_management_action" model="ir.actions.act_window">
        <field name="name">Management</field>
        <field name="res_model">hr.attendance</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_attendance_tree_management_custom"/>
        <field name="context">{}</field>
    </record>

    <!-- Form View -->
    <record id="view_attendance_form_custom" model="ir.ui.view">
        <field name="name">hr.attendance.form.custom</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.hr_attendance_view_form"/>
        <field name="arch" type="xml">
            <field name="check_out" position="after">
                <field name="attendance_type"/>
            </field>
        </field>
    </record>
</odoo>
